// 'use client;'

import { TemplateProps } from './heroBanner'

export default function HeroBannerImageFull({ height }: TemplateProps) {
  //   if (!data) return null

  return <div className="flex items-center gap-4">Hero Banner - Full, {height}</div>
}

// 'use client;'

import { TemplateProps } from './heroBanner'

export default function HeroBannerImageHalf({ height }: TemplateProps) {
  //   if (!data) return null

  return <div className="flex items-center gap-4">Hero Banner - Image Half. {height}</div>
}
